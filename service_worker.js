console.log("This is the service-worker.js");
/*
chrome.action.onClicked.addListener((tab) => {
  chrome.tabs.sendMessage(tab.id, {action: "filter"}, (response) => {
    console.log(response.status);
  });
});
*/